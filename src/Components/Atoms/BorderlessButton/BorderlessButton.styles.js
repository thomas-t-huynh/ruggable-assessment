import styled from "styled-components";

export const StyledButton = styled.button`
  background: none;
  color: inherit;
  border: none;
  outline: inherit;
  font-weight: 600;
  font-size: 14px;
  font-family: inherit;
  cursor: pointer;
`;
